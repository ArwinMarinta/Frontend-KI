// import { useState } from "react";

// const useModalWarning = () => {
//   const [activeModal, setActiveModal] = useState<string | null>(null);
//   const [id, setId] = useState<number | null>(null);

//   return {
//     activeModal,
//   };
// };

// export default useModalWarning;
